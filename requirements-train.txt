# ---------------------------------------------------------------------------
#  VERSI DIKUNCI DAN HARUS SAMA DENGAN requirements-train.txt
#
#  Model scikit-learn disimpan dalam bentuk pickle, yang memuat jejak versi
#  pustaka saat model dibuat. Bila versi saat melatih berbeda dengan versi
#  saat menjalankan, model gagal dibaca. Gejalanya berupa pesan seperti
#  "is not a known BitGenerator module" atau InconsistentVersionWarning.
#
#  Karena itu, setiap kali Anda mengubah versi di sini, ubah juga di
#  requirements-train.txt, lalu latih ulang modelnya dan unggah hasilnya.
# ---------------------------------------------------------------------------
# Untuk MELATIH model di laptop dengan GPU.
#
# Catatan GPU:
#   Linux / WSL2 -> pip install "tensorflow[and-cuda]==2.17.1"
#                   CUDA dan cuDNN ikut terpasang otomatis.
#   Windows asli -> TensorFlow di atas 2.10 tidak mendukung GPU langsung.
#                   Pakai WSL2. Baca docs/PANDUAN_GPU.md
tensorflow[and-cuda]==2.17.1

numpy==1.26.4          # jangan naikkan sendiri, lihat catatan di atas
pandas==2.2.3
scikit-learn==1.7.0   # samakan dengan berkas requirements lainnya  # >=1.7 diperlukan untuk class_weight pada HistGradientBoosting
joblib==1.4.2
matplotlib==3.9.2
requests==2.32.3
python-dotenv==1.0.1

# Pengunduh dataset acuan
kagglehub==0.3.4
datasets==3.1.0
huggingface_hub==0.26.2
roboflow==1.1.48
